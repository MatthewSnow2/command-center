{
  "mcpServers": {
    "matrix-memory": {
      "command": "bash",
      "args": [
        "-c",
        "set -a; source ~/.env.shared; set +a; exec node /home/apexaipc/projects/matrix/dist/mcp/stdio.js"
      ]
    }
  }
}
